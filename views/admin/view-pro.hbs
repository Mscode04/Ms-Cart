<section>
    <div class="container">
        <div class="row">
            <a href="/admin/add-pro" class="btn btn-success ml-auto">Add Product</a>
        </div>
        <table class="table table-dark table-striped mt-2">
            <thead>
                <tr>
                    <th scope="col">title</th>
                    <th scope="col">discriptin</th>
                    <th scope="col">catogory</th>
                    <th scope="col">image</th>
                </tr>
            </thead>
            <tbody>
                {{#each phones}}
                <tr>
                    <td>{{this.title}}</td>
                    <td>{{this.description}}</td>
                    <td>{{this.category}}</td>
                    <td><img style="height: 50px; width:50px" src="{{this.image}}" alt=""></td>
                </tr>
                {{/each}}
            </tbody>
        </table>>
    </div>
</section>